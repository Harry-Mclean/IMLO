IMLO Network – CIFAR-10 Image Classification

This is a Convolutional Neural Network (CNN) built in PyTorch to classify images from the CIFAR-10 dataset. The model has several convolutional layers, pooling, batch normalization, and dropout to improve learning and prevent overfitting.
